{
  "name": "econt-sdk",
  "type": "module",
  "private": true,
  "description": "Versioned OpenAPI schema for the Econt API, with an interactive reference site.",
  "repository": "git@github.com:alttreble/econt-js.git",
  "author": "tedraykov <tedraykov@gmail.com>",
  "license": "MIT",
  "scripts": {
    "generate": "bun ./gen/release.ts",
    "generate:check": "bun ./gen/release.ts --check",
    "docs:build": "rm -rf _site && mkdir -p _site && cp docs/index.html _site/index.html && cp openapi.yaml _site/openapi.yaml",
    "docs:preview": "bun run docs:build && bunx serve _site"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
